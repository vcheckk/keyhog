# Docs: https://docs.pingcap.com/tidbcloud/api/v1beta
# Format: TiDB Cloud uses API keys (public and private key pair) for authentication
# Verify: GET /api/v1beta/clusters with Authorization header returns 200 if valid
# Prefix: none (requires context anchoring)

[detector]
id = "tidb-cloud-credentials"
name = "TiDB Cloud Credentials"
service = "tidbcloud"
severity = "high"
keywords = ["TIDB_CLOUD", "TIDB_PUBLIC_KEY", "TIDB_PRIVATE_KEY", "TIDB_API"]

[[detector.patterns]]
regex = "(?:TIDB[_-]PRIVATE[_-]?KEY)[=:\\s\"'']+([A-Za-z0-9]{32,64})"
description = "TiDB Cloud private API key with context anchor"
group = 1

[[detector.companions]]
name = "TiDB Public Key"
regex = "(?:TIDB[_-]PUBLIC[_-]?KEY)[=:\\s\"'']+[A-Za-z0-9]{16,32}"
within_lines = 5
